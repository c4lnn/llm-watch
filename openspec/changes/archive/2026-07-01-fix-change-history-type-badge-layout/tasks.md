## 1. Change History Table Layout

- [x] 1.1 Update the change history table wrapper to allow horizontal overflow when content is too wide.
- [x] 1.2 Add a stable minimum table width or column width so long details do not collapse compact metadata columns.
- [x] 1.3 Update the type header and type cell in `ChangeHistory.jsx` to avoid wrapping.
- [x] 1.4 Render the type badge as a single inline-flex token with icon and label aligned on one line.

## 2. Verification

- [x] 2.1 Verify the change history page visually with long detail rows and all type values.
- [x] 2.2 Run the frontend production build.
